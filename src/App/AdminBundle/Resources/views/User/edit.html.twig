{% extends 'AppAdminBundle:Layout:admin_layout.html.twig' %}
{% form_theme edit_form 'AppAdminBundle:Form:fields.html.twig' %}

{% block body %}
    
<div class="row">
   <div class="col-lg-12">
       <section class="panel">
           <header class="panel-heading">
               Édition de l'utilisateur
           </header>
          <div class="panel-body">
            <form method="post" class="form-horizontal tasi-form" action="{{ path ('user_update', {'id' : entity.id}) }}">

              

               {{ form(edit_form) }}
              <!-- Ajout script TinyMCE : en bas de page -->
           </div>
            </form>
            <ul class="record_actions">
               <li>
                   <a href="{{ path('user_list') }}">
                       Back to the list
                   </a>
                   
               </li>
            </ul>
      
       </section>
   </div>
</div>
{% endblock %}
